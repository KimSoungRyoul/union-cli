# Project Goal

## 비전
YAML 선언 한 장으로 팀 전용 통합 CLI를 만드는 프레임워크.

## 핵심 원칙
1. **선언형**: 코드 없이 YAML manifest만으로 CLI 커맨드 정의
2. **멀티 프로바이더**: HTTP API, CLI wrapping, Python SDK, JS 모듈을 하나의 CLI로 통합
3. **제로 보일러플레이트**: `npm run build` 한 번이면 커맨드 생성 + 자동완성 + 도움말 + 인증 전부 동작
